= Metrics Logging Dashboards

span.workshop-time-badge[~18 min]

NOTE: For the ROSA/AWS vs lab mapping see xref:00-index.adoc#hybrid-callout[Module 00 — hybrid callout table].

== Overview

OpenShift observability spans cluster metrics, logs, traces, and custom dashboards federated across ACM-managed clusters. Red Hat builds on Prometheus, Loki or Elasticsearch patterns, Grafana, and OpenTelemetry Instrumentation CRs so application teams inherit platform-wide collectors without sidecar sprawl on every pod.

This workshop deploys multicluster Grafana dashboards on the hub, OpenTelemetry collectors via `components/opentelemetry/`, and Kafka Console for IE topic inspection. As `pass:[%USER_NAME%]`, filter dashboards to your namespace and correlate latency spikes with mesh traces in module 17.

Executives should connect this module to module 21 (Kubecost): metrics prove SLO compliance while cost metrics prove efficiency — both required for hybrid FinOps. Use Showroom `oc` to list `GrafanaDashboard` CRs and confirm IE workloads emit scrape targets.

== Lab access — URLs & credentials

NOTE: *Workshop login* — pass:[%USER_NAME%] / `Welcome123!` (Keycloak for Developer Hub; htpasswd for OpenShift Console on hub, east, and west; DevSpaces on east spoke). OAuth products use the same OpenShift identity.

[cols="2,3,2"]
|===
| Service | URL | Credentials

| Grafana | `https://grafana.{hub_domain}` | OpenShift OAuth
| Kiali | `https://kiali-openshift-cluster-observability-operator.{hub_domain}` | OpenShift OAuth
| OpenShift Console — Observe | `https://console-openshift-console.{hub_domain}/observe/metrics` | Workshop user
|===

== Show and Tell

. Open multicluster Grafana dashboard filtered to IE namespace.
. Show Kafka Console and a sample OTEL trace (or metrics gap if trace pending).
. Link metrics SLO story to upcoming Kubecost module.

== Your TODO (user pass:[%USER_NAME%])

* [ ] Open a multicluster Grafana dashboard for your IE namespace
* [ ] Inspect Kafka Console topics for sensor data
* [ ] Save progress at the end of this module

== Live lab

* Showroom integrated `oc` terminal — use hub context unless module says east/west.

== YAML behind the scenes

[cols="2,3,1"]
|===
| UI action | Git source | Kind

| Grafana dashboards | components/grafana-dashboards/ | ConfigMap |
| OTEL | components/opentelemetry/ | Instrumentation |
| Kafka Console | components/kafka-console/ | Deployment |
|===

[source,yaml]
----
# Open Grafana from ConsoleLink; filter by cluster label
----

Verify:

[source,bash]
----
oc get grafanadashboard -A 2>/dev/null | head -5; oc get route -n openshift-cluster-observability-operator 2>/dev/null | head -3
----

== Verify

[cols="1,2,1"]
|===
| Check | Action | Expected

| Progress | Save checkboxes below | `POST /api/progress` returns OK
|===


++++
<div class="workshop-progress" data-module="15-observability">
  <label><input type="checkbox" data-completed> I completed this module</label>
  <label><input type="checkbox" data-interest> I want to learn more</label>
  <button type="button" onclick="saveWorkshopProgress('15-observability')">Save progress</button>
</div>
++++


TIP: Plan B — shared demo in Developer Hub → System `hybrid-mesh-shared-demos` if scaffolder fails.


++++
<div class="workshop-next-nav">
  <a href="16-openshift-gitops.html">Next → 16 openshift gitops</a>
</div>
++++

