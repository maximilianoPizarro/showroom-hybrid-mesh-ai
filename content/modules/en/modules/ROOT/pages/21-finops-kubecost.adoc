= FinOps with Kubecost

span.workshop-time-badge[~15 min]

NOTE: For the ROSA/AWS vs lab mapping see xref:00-index.adoc#hybrid-callout[Module 00 — hybrid callout table].

== Overview

Kubecost on OpenShift allocates Kubernetes spend by namespace, label, and cluster — federating data from hub primary and spoke agents into MinIO-backed ETL storage. Platform teams charge back factory edge tenants and compare ROSA node costs versus on-prem depreciation using consistent Kubernetes unit economics.

In this lab, Kubecost deploys from `components/kubecost/` with agents on east/west reporting to the hub primary. Filter allocations to namespaces owned by `pass:[%USER_NAME%]` and correlate idle capacity with Kairos recommendations from module 14.

FinOps closes the executive loop from module 01: hybrid strategy without cost visibility fails in CFO review. Kubecost complements AWS Cost Explorer tags on ROSA by exposing pod-level waste inside the cluster boundary.

== Show and Tell

. Kubecost UI: allocation by namespace for `pass:[%USER_NAME%]`.
. Show federated cluster dropdown (hub + spokes).
. Compare idle cost with Kairos over-provision narrative.

== Your TODO (user pass:[%USER_NAME%])

* [ ] Open Kubecost and filter allocations to your namespace
* [ ] Compare hub vs spoke cluster costs in federated view
* [ ] Save progress at the end of this module

== Live lab

* Showroom integrated `oc` terminal — use hub context unless module says east/west.

== YAML behind the scenes

[cols="2,3,1"]
|===
| UI action | Git source | Kind

| Kubecost | components/kubecost/ | CostAnalyzer |
| MinIO ETL | components/industrial-edge-minio/ | Bucket |
|===

[source,yaml]
----
# Free tier: single-cluster Kubecost on hub
----

Verify:

[source,bash]
----
oc get deploy -n kubecost kubecost-cost-analyzer
----

== Verify

[cols="1,2,1"]
|===
| Check | Action | Expected

| Progress | Save checkboxes below | `POST /api/progress` returns OK
|===


++++
<div class="workshop-progress" data-module="21-finops-kubecost">
  <label><input type="checkbox" data-completed> I completed this module</label>
  <label><input type="checkbox" data-interest> I want to learn more</label>
  <button type="button" onclick="saveWorkshopProgress('21-finops-kubecost')">Save progress</button>
</div>
++++


TIP: Plan B — shared demo in Developer Hub → System `hybrid-mesh-shared-demos` if scaffolder fails.


++++
<div class="workshop-next-nav">
  <a href="22-openshift-ai.html">Next → 22 openshift ai</a>
</div>
++++

