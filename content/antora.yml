# Shared runtime attributes — merged from user_data at build (hub_domain, east/west, user_name).
name: workshop-runtime
title: Workshop runtime attributes
version: ~
asciidoc:
  attributes:
    hub_domain: "%HUB_DOMAIN%"
    east_domain: "%EAST_DOMAIN%"
    west_domain: "%WEST_DOMAIN%"
    user_name: "%USER_NAME%"
