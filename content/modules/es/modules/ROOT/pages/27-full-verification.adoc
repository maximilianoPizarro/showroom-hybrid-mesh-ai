= Verificación completa

++++
<span class="workshop-time-badge">~15 min</span>
++++

NOTE: Para la tabla ROSA/AWS vs lab ver xref:00-index.adoc#hybrid-callout[Módulo 00 — tabla comparativa].
== Panorama

La verificación full stack confirma comprensión narrativa Parte A y resultados hands-on Parte B para `{user_name}`: redirect de registro, terminal Showroom `oc`, visibilidad spoke ACM, sync IE, asignaciones Kubecost, DSC Ready, salud NeuroFace y persistencia API de progreso.

Ejecuta `bash scripts/verify-workshop-e2e.sh` desde el repo o sigue el checklist en `verification/progress-checklist.yaml`. El servicio de registro en `https://workshop-registration.{hub_domain}` y el endpoint de progreso Showroom (`POST /api/progress`) deben aceptar tus completados de módulo — facilitadores usan progreso agregado para marcar ritmo de sala.

Trata este módulo como puerta de graduación: si algún check falla, revisita el módulo enlazado o cambia a demos compartidos Plan B documentados en Developer Hub System `hybrid-mesh-shared-demos`.

== Acceso al producto — URLs y credenciales

NOTE: *Login del taller* — {user_name} / `Welcome123!` (Keycloak en Developer Hub; htpasswd en consola OpenShift hub/east/west; DevSpaces en spoke east). Productos OAuth usan la misma identidad OpenShift.

.Re-registro si hace falta
* `https://workshop-registration.{hub_domain}/` → {user_name}

.Checklist URLs (todos los productos)
[cols="2,3,2"]
|===
| Producto | URL | Credenciales

| Showroom | `https://showroom-showroom.{hub_domain}/?USER_NAME={user_name}` | Usuario registrado
| Developer Hub | `https://developer-hub.{hub_domain}` | Usuario taller
| Industrial Edge | `https://industrial-edge.{hub_domain}` | Pública (sin login)
| Kuadrant + APIs | `https://developer-hub.{hub_domain}/kuadrant` + `https://workshop-apis.{hub_domain}` | API key
| NeuroFace | `https://neuroface.{hub_domain}` | Pública (sin login)
| Kubecost | `https://kubecost.{hub_domain}` | OAuth OpenShift
|===

== Demostración

. Recorrer ítems checklist y ejecutar extracto script verify en vivo.
. Confirmar guardado API progreso desde UI checkbox Showroom.
. Celebrar completado; compartir rutas Plan B para checks fallidos.

== Tu TODO (usuario {user_name})

* [ ] Completar checklist en `verification/progress-checklist.yaml`
* [ ] Ejecutar o revisar salida de `scripts/verify-workshop-e2e.sh`
* [ ] Guardar progreso al final de este módulo

== Lab en vivo

* Terminal `oc` integrada en Showroom — usa contexto hub salvo que el módulo indique east/west.

== YAML behind the scenes

[cols="2,3,1"]
|===
| UI action | Git source | Kind

| Progress API | workshop-registration | POST /api/progress |
| E2E script | scripts/verify-workshop-e2e.sh | Shell |
| Checklist | verification/progress-checklist.yaml | YAML |
|===

[source,yaml]
----
# Checks: registration, showroom, ACM clusters, IE deploy, NeuroFace route
----

Verify:

[source,bash]
----
bash scripts/verify-workshop-e2e.sh 2>/dev/null || oc get managedclusters
----

== Verificar

[cols="1,2,1"]
|===
| Check | Action | Expected

| Progress | Save checkboxes below | `POST /api/progress` returns OK
|===


++++
<div class="workshop-progress" data-module="27-full-verification">
  <label><input type="checkbox" data-completed> Completé este módulo</label>
  <label><input type="checkbox" data-interest> Me interesa profundizar</label>
  <button type="button" onclick="saveWorkshopProgress('27-full-verification')">Guardar progreso</button>
</div>
++++


TIP: Plan B — demo compartido en Developer Hub → System `hybrid-mesh-shared-demos` si falla el scaffolder.


++++
<div class="workshop-next-nav">
  <a href="28-agent-browser-recording.html">Siguiente → 28 agent browser recording</a>
</div>
++++

