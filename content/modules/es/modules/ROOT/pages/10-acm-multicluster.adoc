= Flota multicluster y ACM

++++
<span class="workshop-time-badge">~15 min</span>
++++

image::images/10-acm-multicluster.png[ACM multicluster fleet management,600]

NOTE: Para snippets AWS/Azure ver xref:00-index.adoc#hybrid-integration[Módulo 00 — integración híbrida].
== Panorama

Red Hat Advanced Cluster Management for Kubernetes convierte OpenShift en plano de control de flota: importar spokes, aplicar políticas, visualizar salud y delegar GitOps con RBAC consistente. ManagedCluster y agentes Klusterlet reflejan cómo clusters ROSA y on-prem se unen al hub de gobernanza sin compartir kube-admin ampliamente.

En este lab, abre ACM Clusters en el hub y localiza east y west — cada spoke se bootstrapeó desde manifiestos GitOps de `components/acm-hub-spoke/`. Como `%USER_NAME%`, tus cargas aterrizan en east por defecto; Topology en Developer Hub usa APIs OCM para mostrar el mismo grafo que ACM.

Este módulo establece el modelo mental para cada ejercicio Parte B: el hub posee ingress, política, agregación FinOps y planos de control IA; los spokes ejecutan Industrial Edge y namespaces con alcance de usuario. Verifica con `oc get managedclusters` desde la terminal Showroom.

== Acceso al producto — URLs y credenciales

NOTE: *Login del taller* — %USER_NAME% / `Welcome123!` (Keycloak en Developer Hub; htpasswd en consola OpenShift hub/east/west; DevSpaces en spoke east). Productos OAuth usan la misma identidad OpenShift.

[cols="2,3,2"]
|===
| Servicio | URL | Credenciales

| ACM Clusters | `https://console-openshift-console.%HUB_DOMAIN%/multicloud/infrastructure/clusters` | Usuario taller
| ACM GitOps (Policies) | `https://console-openshift-console.%HUB_DOMAIN%/multicloud/policies` | Usuario taller
| Consola hub | `https://console-openshift-console.%HUB_DOMAIN%` | Usuario taller
|===

== Demostración

. Abrir UI ACM Clusters — identificar spokes east y west.
. Ejecutar `oc get managedclusters` en terminal Showroom en vivo.
. Mostrar Topology Developer Hub reflejando grafo OCM.

== Tu TODO (usuario %USER_NAME%)

* [ ] Ejecutar `oc get managedclusters` e identificar east/west
* [ ] Abrir UI ACM Clusters y Topology Developer Hub para la misma flota
* [ ] Guardar progreso al final de este módulo

== Lab en vivo

* Terminal `oc` integrada en Showroom — usa contexto hub salvo que el módulo indique east/west.

== YAML behind the scenes

[cols="2,3,1"]
|===
| UI action | Git source | Kind

| ACM Clusters UI | components/acm-hub-spoke/ | ManagedCluster |
| GitOpsCluster | components/acm-hub-spoke/templates/gitops-cluster.yaml | GitOpsCluster |
|===

[source,yaml]
----
apiVersion: cluster.open-cluster-management.io/v1
kind: ManagedCluster
metadata:
  name: east
spec:
  hubAcceptsClient: true
----

Verify:

[source,bash]
----
oc get managedclusters && oc get gitopscluster -A
----

== Verificar

[cols="1,2,1"]
|===
| Check | Action | Expected

| Progress | Save checkboxes below | `POST /api/progress` returns OK
|===


++++
<div class="workshop-progress" data-module="10-acm-multicluster">
  <label><input type="checkbox" data-completed> Completé este módulo</label>
  <label><input type="checkbox" data-interest> Me interesa profundizar</label>
  <button type="button" onclick="saveWorkshopProgress('10-acm-multicluster')">Guardar progreso</button>
</div>
++++


TIP: Plan B — demo compartido en Developer Hub → System `hybrid-mesh-shared-demos` si falla el scaffolder.


++++
<div class="workshop-next-nav">
  <a href="11-hybrid-mesh-architecture.html">Siguiente → 11 hybrid mesh architecture</a>
</div>
++++

