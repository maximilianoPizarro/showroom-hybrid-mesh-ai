= LLMs y RAG

span.workshop-time-badge[~22 min]

NOTE: Para la tabla ROSA/AWS vs lab ver xref:00-index.adoc#hybrid-callout[Módulo 00 — tabla comparativa].

== Panorama

Los modelos de lenguaje grande y RAG (retrieval-augmented generation) en OpenShift combinan inferencia centralizada con contexto de dominio desde docs de fábrica, runbooks y logs de sensores — sin exportar datos propietarios a SaaS público. Red Hat Developer Hub Lightspeed asiste desarrolladores en catálogo; agentes Kairos Console responden preguntas de escalado; Continue AI en DevSpaces sugiere código inline usando el mismo backend MaaS.

La arquitectura RAG en manufactura híbrida típicamente indexa PDFs y SOPs en un vector store (elección del cliente) mientras el LLM corre en OpenShift AI ModelMesh o MaaS externo. En este lab, configuras el despliegue Lightspeed en `components/developer-hub/templates/lightspeed.yaml` y pruebas prompts contra el endpoint MaaS compartido — observa latencia y uso de tokens adecuados para restricciones Wi-Fi de planta.

Como `pass:[%USER_NAME%]`, abre Developer Hub, dispara Lightspeed en un Component de catálogo y compara respuestas con Continue AI en un workspace DevSpaces. El módulo 25 extiende la misma ruta MaaS a chat multimodal NeuroFace — demostrando un modelo de gobernanza para superficies IA de dev, ops y usuario final.

== Show and Tell

. Disparar Developer Hub Lightspeed en Component catálogo en vivo.
. Opcional sugerencia inline Continue AI DevSpaces usando MaaS.
. Discutir ubicación índice RAG (elección cliente) vs LLM en OpenShift AI.

== Tu TODO (usuario pass:[%USER_NAME%])

* [ ] Disparar Lightspeed en Developer Hub sobre entidad catálogo
* [ ] Enviar un prompt MaaS y anotar latencia/calidad de respuesta
* [ ] Guardar progreso al final de este módulo

== Lab en vivo

* Terminal `oc` integrada en Showroom — usa contexto hub salvo que el módulo indique east/west.

== YAML behind the scenes

[cols="2,3,1"]
|===
| UI action | Git source | Kind

| Lightspeed | components/developer-hub/ | Plugin |
| MaaS | openshift-ai-hub | Credentials |
|===

[source,yaml]
----
# Developer Hub → Lightspeed uses MaaS llama-scout-17b
----

Verify:

[source,bash]
----
oc get deploy -n developer-hub 2>/dev/null | grep -i lightspeed || echo 'Lightspeed via RHDH plugin'
----

== Verificar

[cols="1,2,1"]
|===
| Check | Action | Expected

| Progress | Save checkboxes below | `POST /api/progress` returns OK
|===


++++
<div class="workshop-progress" data-module="23-llm-rag">
  <label><input type="checkbox" data-completed> Completé este módulo</label>
  <label><input type="checkbox" data-interest> Me interesa profundizar</label>
  <button type="button" onclick="saveWorkshopProgress('23-llm-rag')">Guardar progreso</button>
</div>
++++


TIP: Plan B — demo compartido en Developer Hub → System `hybrid-mesh-shared-demos` si falla el scaffolder.


++++
<div class="workshop-next-nav">
  <a href="24-text-ai-predictive.html">Siguiente → 24 text ai predictive</a>
</div>
++++

