= Service Mesh

++++
<span class="workshop-time-badge">~18 min</span>
++++

image::17-service-mesh.png[OpenShift Service Mesh ambient,960]

NOTE: Para snippets AWS/Azure ver xref:00-index.adoc#hybrid-integration[Módulo 00 — integración híbrida].



== Panorama


NOTE: **Ritmo del taller (~4 h hands-on)** — Cada módulo incluye pasos **Solo visualización** (~5–10 min) y **Hands-on** (lab completo). Omite Hands-on para un tour ejecutivo ~90 min.

OpenShift Service Mesh 3 introduce modo ambient: una capa ztunnel compartida maneja mTLS y telemetría L4 sin inyectar sidecars en cada pod por defecto. Kiali visualiza grafos de tráfico; la config mesh habilita trazas distribuidas para microservicios Industrial Edge en namespaces spoke east.

En este taller, OSSM3 se suscribe vía `components/operators/templates/servicemeshoperator3.yaml` y se configura en modo dataplane ambient en namespaces IE — excluyendo `stackrox` donde ACS requiere visibilidad de red directa. Compara con despliegues ROSA usando App Mesh o meshes de terceros: OpenShift mantiene CRDs y políticas mesh nativas al ciclo de vida de plataforma.

Usa Kiali desde la consola OpenShift para ver tráfico en vivo de despliegues de `%USER_NAME%` y validar mTLS entre line-dashboard y servicios orientados a Kafka. El módulo 17 combina con dashboards de observabilidad del módulo 15 para análisis de latencia end-to-end.

=== Profundizar

* link:https://docs.redhat.com/en/documentation/openshift_container_platform/4.16/html/service_mesh/index[OpenShift Service Mesh]
* link:https://istio.io/latest/docs/[Istio documentation]
* link:https://docs.redhat.com/en/documentation/openshift_container_platform/4.16/html/service_mesh/ambient-overview[Ambient mesh overview]
* link:https://kiali.io/docs/[Kiali service mesh observability]


== Características, beneficios y configuración cloud

=== Características

* **Service Mesh 3** ambient + Kiali.

=== AWS App Mesh

[source,bash]
----
aws appmesh create-mesh --mesh-name factory-mesh
----


== Acceso al producto — URLs y credenciales

NOTE: *Login del taller* — %USER_NAME% / `Welcome123!` (Keycloak en Developer Hub; htpasswd en consola OpenShift hub/east/west; DevSpaces en spoke east). Productos OAuth usan la misma identidad OpenShift.

[cols="2,3,2"]
|===
| Servicio | URL | Credenciales

| Kiali | link:https://kiali-openshift-cluster-observability-operator.%HUB_DOMAIN%[Kiali] | OAuth OpenShift
| Industrial Edge (apps en mesh) | link:https://industrial-edge.%HUB_DOMAIN%[Industrial Edge (apps en mesh)] | Pública (sin login)
| Consola OpenShift — mesh | ACM → `east` → Operators → Service Mesh | Usuario taller
|===

== Demostración

. Abrir grafo Kiali para namespace IE — señalar aristas ztunnel ambient.
. Notar exclusión `stackrox` del mesh ambient.
. Opcional: mostrar icono candado mTLS en aristas de servicio.
== Tu TODO (usuario %USER_NAME%)

* [ ] Abrir Kiali y ver tráfico de tus despliegues IE
* [ ] Confirmar mesh ambient habilitado en namespace IE (no stackrox)
* [ ] Guardar progreso al final de este módulo

== Lab en vivo

* Terminal `oc` integrada en Showroom — usa contexto hub salvo que el módulo indique east/west.

== Dónde está definido este lab

NOTE: Las rutas referencian el repo GitOps `platform-hub-spoke-config` desplegado en **este** cluster. No copies fragmentos como manifiestos independientes — usa los enlaces de consola y verifica con `oc`.

[cols="2,3"]
|===
| UI / capability | Source in GitOps repo

| OSSM3 subscription | `components/servicemeshoperator3/` |
| Kiali | `components/kiali/` |
|===

Verificar en la terminal Showroom:

[source,bash]
----
oc get istio -n istio-system 2>/dev/null; oc get kiali -A 2>/dev/null | head -3
----

== Verificar

[cols="1,2,1"]
|===
| Check | Action | Expected

| Progress | Save checkboxes below | `POST /api/progress` returns OK
|===


++++
<div class="workshop-progress" data-module="17-service-mesh">
  <label><input type="checkbox" data-completed> Completé este módulo</label>
  <label><input type="checkbox" data-interest> Me interesa profundizar</label>
  <button type="button" onclick="saveWorkshopProgress('17-service-mesh')">Guardar progreso</button>
</div>
++++


TIP: Plan B — demo compartido en Developer Hub → System `hybrid-mesh-shared-demos` si falla el scaffolder.


++++
<div class="workshop-next-nav">
  <a href="18-scalability.html">Siguiente → 18 scalability</a>
</div>
++++

