= Hybrid Mesh AI Workshop

++++
<span class="workshop-time-badge">~10 min</span>
++++

image::00-index-hybrid-mesh.png[Hybrid Mesh AI hub-spoke architecture diagram — hub cluster managing east and west spokes via ACM,960]

== Welcome

Welcome to the *Hybrid Mesh AI Workshop* — a dual-track experience that mirrors how Red Hat customers run hybrid cloud on OpenShift.

*Part A (modules 01–05)* is executive-oriented: it covers hybrid cloud strategy, ROSA architecture, security at scale, AWS AI integration, and real customer cases. No hands-on required — facilitators can demo while the audience follows along.

*Part B (modules 10–28)* is fully hands-on on a live RHDP hub-spoke fleet provisioned for this workshop. Each module targets a specific product area and includes a `verify` step to confirm your work.

The lab simulates a production hybrid mesh: a hub cluster (OpenShift on AWS) managing two spoke clusters (east and west) via ACM, with ambient service mesh, GitOps, and an AI inference layer backed by OpenShift AI + MaaS.

Register at `https://workshop-registration.{hub_domain}` to receive your lab identity (`userN`). The Showroom at `https://showroom-showroom.{hub_domain}` embeds an authenticated `oc` terminal — no local kubeconfig required. If scaffolding fails, use *Plan B* shared demos in Developer Hub → System `hybrid-mesh-shared-demos`.

++++
<p class="workshop-register-cta">
  <a id="workshop-register-cta-main" class="workshop-register-btn"
     href="https://workshop-registration.%HUB_DOMAIN%/?USER_NAME=%USER_NAME%"
     target="_blank" rel="noopener noreferrer">Register for lab access →</a>
  <span class="workshop-register-hint">Already registered? Open this page with <code>USER_NAME=userN</code> in the URL.</span>
</p>
++++

== Production mapping — ROSA/AWS vs this lab

This workshop mirrors a real Red Hat customer deployment. The table below maps each production component (left) to its lab equivalent (right) so you can relate every hands-on step to a production pattern.

[cols="1,1", options="header"]
|===
| In production (ROSA + AWS) | In this lab (RHDP hub-spoke)

| ROSA managed cluster in AWS account (Multi-AZ, SLA 99.95%)
| Hub pre-provisioned by RHDP; east/west spokes registered via ACM

| ROSA MachineSets + Cluster Autoscaler
| Kairos `SmartScalingPolicy` + HPA + Kafka KEDA triggers

| AWS Security Groups + IAM OIDC + VPC NetworkPolicy
| OVN-Kubernetes `NetworkPolicy` + ACS + Kuadrant `AuthPolicy`

| Amazon Bedrock / SageMaker inference endpoints
| OpenShift AI `InferenceService` + MaaS + NeuroFace + Lightspeed

| AWS Cost Explorer + resource tags
| Kubecost federated ETL — hub aggregates east/west spend

| Route 53 + ALB + WAF
| Hub Gateway API + Skupper cross-cluster tunnel + Connectivity Link

| AWS CodePipeline / CodeBuild
| OpenShift GitOps (Argo CD) + Developer Hub software templates

| Amazon EKS add-ons / managed node groups
| ACM `ManagedCluster` + `PlacementRule` + `ApplicationSet`
|===

== Hub-spoke architecture

The workshop fleet is a three-cluster hub-spoke topology managed by ACM:

[source,yaml]
----
# ACM ManagedCluster — hub registers spoke clusters automatically
apiVersion: cluster.open-cluster-management.io/v1
kind: ManagedCluster
metadata:
  name: east
  labels:
    cloud: Amazon
    region: us-east-1
    env: workshop
spec:
  hubAcceptsClient: true
----

[source,yaml]
----
# GitOpsCluster — lets Argo CD on the hub deploy to spokes
apiVersion: apps.open-cluster-management.io/v1beta1
kind: GitOpsCluster
metadata:
  name: workshop-gitops
  namespace: openshift-gitops
spec:
  argoServer:
    cluster: local-cluster
    argoNamespace: openshift-gitops
  placementRef:
    kind: Placement
    apiGroup: cluster.open-cluster-management.io
    name: all-workshop-clusters
----

The hub runs: ACM, OpenShift GitOps (Argo CD), Developer Hub, OpenShift AI, Service Mesh control plane, Skupper, Kuadrant, ACS Central, Grafana, and Kubecost. +
The east spoke runs: Industrial Edge workloads, DevSpaces, Kairos, and spoke-local Argo CD agents. +
The west spoke runs: additional workload replicas and demonstrates cross-cluster traffic via Skupper.

== Service mesh & traffic flow

The workshop uses OpenShift Service Mesh 3 in *ambient mode* (no sidecars). Traffic between hub and spokes crosses a Skupper tunnel exposed via Gateway API:

[source,yaml]
----
# HTTPRoute — routes /api/inference from hub gateway to MaaS on east spoke via Skupper
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: maas-inference-route
  namespace: hybrid-mesh-gateway
spec:
  parentRefs:
    - name: hub-gateway
      namespace: hybrid-mesh-gateway
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/inference
      backendRefs:
        - name: skupper-maas-backend   # Skupper connector bridging to east spoke
          port: 8080
----

[source,yaml]
----
# AuthorizationPolicy — zero-trust: only gateway SA can reach MaaS
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: maas-allow-gateway
  namespace: maas-workshop
spec:
  selector:
    matchLabels:
      app: maas-api
  rules:
    - from:
        - source:
            principals:
              - cluster.local/ns/hybrid-mesh-gateway/sa/hub-gateway
----

== OpenShift AI — Model as a Service

The AI layer is a shared LLM endpoint (MaaS) deployed on the hub via the OpenShift AI operator. All AI modules (22–26) consume this endpoint.

[source,yaml]
----
# DataScienceCluster — installs all RHOAI components on the hub
apiVersion: datasciencecluster.opendatahub.io/v1
kind: DataScienceCluster
metadata:
  name: default-dsc
spec:
  components:
    dashboard: {managementState: Managed}
    workbenches: {managementState: Managed}
    modelmeshserving: {managementState: Managed}
    datasciencepipelines: {managementState: Managed}
    kserve: {managementState: Managed}
----

[source,yaml]
----
# Secret — OpenAI-compatible base URL pointing to the in-cluster MaaS
apiVersion: v1
kind: Secret
metadata:
  name: maas-api-key
  namespace: maas-workshop
stringData:
  OPENAI_API_BASE: "http://maas-api.maas-workshop.svc.cluster.local:8080/v1"
  OPENAI_API_KEY: "workshop-key-{user_name}"
----

Any application that speaks the OpenAI REST API can consume MaaS without code changes — just point `OPENAI_API_BASE` to the in-cluster service.

== Kuadrant API gateway

Kuadrant manages API rate limiting and auth policies across the hub gateway. Each workshop user gets their own API key scoped to a plan:

[source,yaml]
----
# APIProduct — exposes the workshop AI endpoints under a single managed product
apiVersion: kuadrant.io/v1alpha1
kind: APIProduct
metadata:
  name: workshop-ai-api
  namespace: kuadrant-system
spec:
  hosts:
    - workshop-apis.{hub_domain}
  APIs:
    - name: maas-inference-route
      namespace: hybrid-mesh-gateway
----

[source,yaml]
----
# TokenRateLimitPolicy — limits per API key to 100 req/min for workshop users
apiVersion: kuadrant.io/v1alpha1
kind: TokenRateLimitPolicy
metadata:
  name: workshop-rate-limit
  namespace: kuadrant-system
spec:
  targetRef:
    group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: maas-inference-route
  limits:
    per-user:
      rates:
        - limit: 100
          duration: 1
          unit: minute
      counters:
        - auth.identity.username
----

== Lab access — URLs & credentials

NOTE: *Workshop login* — {user_name} / `Welcome123!` (Keycloak Developer Hub; htpasswd OpenShift hub/east/west; DevSpaces east). OAuth products use the same OpenShift identity.


.Workshop entry points
[cols="2,3,2"]
|===
| Service | URL | Credentials

| Registration | link:https://workshop-registration.{hub_domain}/?USER_NAME={user_name}[Register for lab access] | Email → assigns {user_name}
| Showroom (this site) | link:https://showroom-showroom.{hub_domain}/?USER_NAME={user_name}[Showroom home] | After registration redirect
| OpenShift Console (hub) | link:https://console-openshift-console.{hub_domain}[OpenShift Console] | Workshop user
| Developer Hub | link:https://developer-hub.{hub_domain}[Developer Hub] | Workshop user
| Mailpit (IE alerts) | link:https://mailpit.{hub_domain}[Mailpit IE] | Public (no login)
| Mailpit (Templates) | link:https://mailpit-templates.{hub_domain}[Mailpit Templates] | Public (no login)
|===

.Product catalog — quick links
[cols="2,3,2"]
|===
| Product | URL | Credentials

| ACM fleet | link:https://console-openshift-console.{hub_domain}/multicloud/infrastructure/clusters[ACM fleet] | Workshop user
| Industrial Edge | link:https://industrial-edge.{hub_domain}[Industrial Edge UI] | Public (no login)
| Gitea | link:https://gitea-gitea.{hub_domain}[Gitea] | Workshop user or facilitator
| Grafana | link:https://grafana.{hub_domain}[Grafana] | OpenShift OAuth
| Kiali | link:https://kiali-openshift-cluster-observability-operator.{hub_domain}[Kiali] | OpenShift OAuth
| Kairos Console | link:https://kairos-console-kairos-system.{hub_domain}[Kairos] | OpenShift OAuth
| ACS Central | link:https://central-stackrox.{hub_domain}[ACS Central] | OpenShift OAuth
| Kuadrant API keys | link:https://developer-hub.{hub_domain}/kuadrant[Kuadrant UI] | Workshop user
| Workshop APIs | link:https://workshop-apis.{hub_domain}[Workshop APIs] | Kuadrant API key (module 20)
| Kafka Console | link:https://kafka-console.{hub_domain}[Kafka Console] | OpenShift OAuth
| Kubecost | link:https://kubecost.{hub_domain}[Kubecost] | OpenShift OAuth
| Quay | link:https://quay-registry.{hub_domain}[Quay] | OpenShift OAuth
| DevSpaces (east) | link:https://devspaces.{east_domain}[DevSpaces east] | Workshop user
| OpenShift AI dashboard | link:https://rhods-dashboard-redhat-ods-applications.{hub_domain}[ODS Dashboard] | OpenShift OAuth
| Skupper observer | link:https://field-content-skupper-network-observer-service-interconnect.{hub_domain}[Skupper] | OpenShift OAuth
| NeuroFace | link:https://neuroface.{hub_domain}[NeuroFace] | Public (no login)
|===

== Red Hat product catalog in this workshop

=== OpenShift Container Platform
* **Advanced Cluster Management (ACM)** — fleet governance, policies, and GitOps-driven placement across hub and spokes.
* **Red Hat Advanced Cluster Security (ACS)** — runtime threat detection and compliance (`stackrox`; outside ambient mesh).
* **Quay** — container registry referenced by GitOps and template outputs.
* **OpenShift GitOps** — Argo CD Applications and ApplicationSets sync workshop components from Git.
* **Red Hat OpenShift Dev Spaces** — cloud IDE on the east spoke for Kaoto, Continue AI, and templates.
* **OpenShift Pipelines** — Tekton pipelines in Industrial Edge and template workflows.
* **OpenShift distributed tracing / cluster observability** — metrics, logging, and traces (Grafana, Kiali, OTEL).
* **OpenShift Service Mesh 3** — ambient mesh with ztunnel and L7 policy for Industrial Edge apps.

=== Red Hat Application Foundation
* **Apache Camel on Kubernetes** — integrations and CDC pipelines in the Industrial Edge demo.
* **Connectivity Link** — Gateway API ingress/egress patterns at the hub gateway (Skupper, HTTPRoute, external backends).
* **Kuadrant** — APIProduct catalog, AuthPolicy, PlanPolicy, and TokenRateLimitPolicy (separate from Connectivity Link).

=== Red Hat Advanced Developer Suite
* **Developer Hub (Backstage)** — software catalog, scaffolder templates, and Topology for multicluster views.
* **Software Templates** — golden paths for Industrial Edge, Camel, CNV, and OpenShift AI workspaces.

=== OpenShift AI
* **DataScienceCluster (DSC)** — unified operator for notebooks, serving, and model mesh on the hub.
* **Model-as-a-Service (MaaS)** — shared LLM endpoint consumed by NeuroFace, Lightspeed, and DevSpaces.
* **NeuroFace** — webcam object/face detection plus chat backed by MaaS.

=== OpenShift Virtualization
* **Container-native virtualization (CNV)** — VM workloads alongside containers via the workshop CNV demo template.

=== Community & third-party
* **Kairos Community** — SmartScalingPolicy recommendations for Industrial Edge sensor workloads (community operator).
* **Gitea** — per-user org `ws-{user_name}` stores scaffolded Git repos for Argo CD.
* **MinIO** — object storage for model artifacts and data lake patterns.
* **Kubecost** — FinOps allocations on the hub cluster.

== Prerequisites

* Modern web browser (Chrome or Firefox recommended) with webcam access for NeuroFace modules.
* Access to the OpenShift console on the workshop hub — launch **Hybrid Mesh AI Workshop** from the Application menu.
* Workshop registration at link:https://workshop-registration.{hub_domain}/?USER_NAME={user_name}[workshop registration] to obtain `{user_name}` and Showroom redirect.
* Optional: use the embedded Showroom terminal for `oc` commands; no local kubeconfig required for Part B.

== Dual agenda

*Part A — Executive (modules 01–05, ~60 min total)* is facilitator-led. No cluster access required. Audience follows slide decks and live demos.

[cols="1,2,1", options="header"]
|===
| Module | Topic | Duration
| 01 | Hybrid Cloud Strategy | ~10 min
| 02 | ROSA Architecture & Benefits | ~15 min
| 03 | Security & Scale in Hybrid | ~10 min
| 04 | AWS Services & AI Integration | ~15 min
| 05 | Real Cases & Roadmap | ~10 min
|===

*Part B — Hands-on (modules 10–28, ~3 h total)* runs on a live RHDP hub-spoke fleet. Each module includes a `verify` step with `oc` commands to confirm your work.

[cols="1,2,1", options="header"]
|===
| Modules | Topic | Duration
| 10–14 | Foundation — ACM fleet, hybrid mesh, software templates, Industrial Edge, Kairos scaling | ~45 min
| 15–18 | Operations — observability, GitOps, service mesh, HPA + Kafka | ~40 min
| 19–21 | Security & FinOps — NetworkPolicy, ACS/Kuadrant, Kubecost | ~30 min
| 22–26 | Hybrid Mesh AI — OpenShift AI, LLMs, RAG, NeuroFace, end-user apps | ~50 min
| 27–28 | Verification & agent browser recording runbook | ~15 min
|===

NOTE: Modules 29–30 (verification & Agent Browser) are **facilitator/agent tasks** only — see `showroom-hybrid-mesh-ai/verification/`. Each Part B module supports **Overview-only** (~90 min) vs **Hands-on** (~3 h).

== Registration & Plan B demos

Register before starting Part B to receive your lab identity (`userN`) and have Showroom pre-configure your `oc` session:

. Open `https://workshop-registration.{hub_domain}` and submit your name.
. You will be redirected to Showroom with `?USER_NAME=userN` in the URL — this activates your personal lab context.
. Keep the Showroom tab open throughout Part B. It embeds a live `oc` terminal authenticated to the hub cluster.

*Plan B — if the Developer Hub scaffolder fails:*

- Open Developer Hub → *Create* → search for System `hybrid-mesh-shared-demos`.
- All pre-built application templates are available there without scaffolding.
- NeuroFace is available directly at `https://neuroface.{hub_domain}` (does not require LibreChat).
- The OpenShift console ApplicationMenu shows *Hybrid Mesh AI Workshop* as a shortcut to all lab URLs.

[NOTE]
====
If you close the Showroom tab, re-open it with `?USER_NAME=userN` to restore your session context. Your `oc` credentials are valid for the full workshop duration.
====

[[hybrid-integration]]
== Hybrid cloud integration notes

Workshop patterns apply to OpenShift on-premises, at the edge, or on public cloud. This lab uses a pre-provisioned RHDP hub-spoke fleet — use the snippets below when you need to provision or attach similar services in AWS or Azure.

=== AWS — attach clusters and integrate services

[source,bash]
----
# Example: create ROSA cluster (or use existing EKS/ROSA) and import to ACM hub
rosa create cluster --cluster-name=factory-edge --region=us-east-1
# ACM: cluster import / ManagedCluster — see module 10
aws iam create-open-id-connect-provider ...   # OIDC for cloud integrations
aws s3 mb s3://my-data-lake                   # analog to MinIO data lake on hub
----

=== Azure — attach AKS and optional AI endpoints

[source,bash]
----
az aks create --resource-group rg-workshop --name aks-edge --node-count 3
# Import to ACM hub (ManagedCluster) — same GitOps placement as east/west spokes
# Azure OpenAI can replace MaaS for inference; lab uses OpenShift AI + MaaS (module 22)
----

Hands-on equivalents: ACM fleet (module 10), Kuadrant API gateway (module 20), OpenShift AI + MaaS (modules 22–25).

== Show and Tell

By the end of this module you should be able to:

* Explain the hub-spoke topology and how it maps to a production ROSA deployment.
* Identify which Red Hat product handles each layer (networking, security, AI, cost).
* Know your lab credentials (`{user_name}` / `Welcome123!`) and where to find all lab URLs.
* Understand when to use Plan B (shared demos) instead of the scaffolder.

Facilitator cues:

. Walk through the *Production mapping* table row by row, asking the audience which AWS service they currently use.
. Show the Showroom terminal live — run `oc get managedclusters` to confirm east and west spokes are available.
. Open Developer Hub and point to System `hybrid-mesh-shared-demos` as the Plan B safety net.

== Your TODO (user `{user_name}`)

Complete these steps before moving to module 01:

* [ ] Read the *Production mapping* table and identify 2 AWS services you use today.
* [ ] Register at `https://workshop-registration.{hub_domain}` and confirm you see `User: {user_name}` in the Showroom header.
* [ ] Open the Showroom terminal and run:
+
[source,bash]
----
# Confirm your lab identity
oc whoami
# Expected: {user_name}

# Confirm both spoke clusters are registered
oc get managedclusters
----
* [ ] Bookmark the Developer Hub URL from the *Lab access* table.
* [ ] Mark this module complete using the button below.

== Verify

Run all of the following from the Showroom embedded terminal. Every command should return the expected output before you proceed to Part A.

[source,bash]
----
# 1. Confirm lab identity
oc whoami
# Expected: user1  (or your assigned userN)

# 2. Confirm hub cluster nodes are Ready
oc get nodes --no-headers | awk '{print $2}' | sort | uniq -c
# Expected: all nodes in Ready state, no NotReady

# 3. Confirm both spoke clusters are registered and available
oc get managedclusters -o custom-columns='NAME:.metadata.name,AVAILABLE:.status.conditions[-1].type,REASON:.status.conditions[-1].reason'
# Expected:
# NAME    AVAILABLE                          REASON
# east    ManagedClusterConditionAvailable   ManagedClusterAvailable
# west    ManagedClusterConditionAvailable   ManagedClusterAvailable

# 4. Confirm OpenShift GitOps (Argo CD) is running
oc get pods -n openshift-gitops -l app.kubernetes.io/name=argocd-server --no-headers
# Expected: 1 pod in Running state

# 5. Confirm OpenShift AI DataScienceCluster is Ready
oc get dsc default-dsc -o jsonpath='{.status.phase}{"\n"}'
# Expected: Ready

# 6. Confirm Developer Hub is reachable
curl -s -o /dev/null -w "%{http_code}" https://developer-hub.{hub_domain}/
# Expected: 200 or 302

# 7. Confirm MaaS inference endpoint is live
curl -s https://maas-api.{hub_domain}/v1/models \
  -H "Authorization: Bearer Welcome123!" | jq '.data[].id'
# Expected: one or more model IDs (e.g. "mistral-7b", "granite-3b")
----

[cols="1,2,2", options="header"]
|===
| # | Check | Expected result
| 1 | `oc whoami` | `{user_name}`
| 2 | `oc get nodes` | All nodes `Ready`
| 3 | `oc get managedclusters` | `east` and `west` both `Available`
| 4 | Argo CD pod | 1 pod `Running` in `openshift-gitops`
| 5 | `oc get dsc` | `phase: Ready`
| 6 | Developer Hub HTTP | `200` or `302`
| 7 | MaaS `/v1/models` | model IDs returned
|===

If any check fails, see the *Plan B* callout above or ask the facilitator.


++++
<div class="workshop-progress" data-module="00-index">
  <label><input type="checkbox" data-completed> I completed this module</label>
  <label><input type="checkbox" data-interest> I want to learn more</label>
  <button type="button" onclick="saveWorkshopProgress('00-index')">Save progress</button>
</div>
++++


TIP: Plan B — shared demo in Developer Hub → System `hybrid-mesh-shared-demos` if scaffolder fails.


++++
<div class="workshop-next-nav">
  <a href="01-hybrid-cloud-strategy.html">Next → 01 hybrid cloud strategy</a>
</div>
++++
