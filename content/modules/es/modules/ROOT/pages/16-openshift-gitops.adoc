= OpenShift GitOps

++++
<span class="workshop-time-badge">~15 min</span>
++++

image::16-openshift-gitops.png[OpenShift GitOps and Argo CD,600]

NOTE: Para snippets AWS/Azure ver xref:00-index.adoc#hybrid-integration[Módulo 00 — integración híbrida].



== Panorama


NOTE: **Ritmo del taller (~4 h hands-on)** — Cada módulo incluye pasos **Solo visualización** (~5–10 min) y **Hands-on** (lab completo). Omite Hands-on para un tour ejecutivo ~90 min.

OpenShift GitOps instala Argo CD como operador gestionado e integra con ApplicationSets ACM para propagar manifiestos hub-to-spoke con destinos seguros por política. Los equipos de plataforma commitean estado deseado a Git; controladores reconcilian drift — la misma disciplina GitOps que clientes ROSA usan al combinar clusters ROSA con repos hub ACM.

En este lab, Applications hub bajo `templates/component-applications.yaml` despliegan servicios compartidos mientras Applications spoke (por ejemplo Industrial Edge) sincronizan desde repos Gitea de usuario creados en el módulo 13. ApplicationSet `industrial-edge-spoke` demuestra generadores matrix apuntando a labels east/west.

Inspecciona estado de sync en UI Argo CD como `%USER_NAME%` e identifica qué revisión de repo disparó tu despliegue. GitOps es la columna operativa: cada módulo de producto (mesh, ACS, IA) resuelve en YAML rastreado en `platform-hub-spoke-config`.


== Acceso al producto — URLs y credenciales

NOTE: *Login del taller* — %USER_NAME% / `Welcome123!` (Keycloak en Developer Hub; htpasswd en consola OpenShift hub/east/west; DevSpaces en spoke east). Productos OAuth usan la misma identidad OpenShift.

[cols="2,3,2"]
|===
| Servicio | URL | Credenciales

| Gitea | link:https://gitea-gitea.%HUB_DOMAIN%[Gitea] | Usuario taller (*Admin Gitea (facilitador):* `gitea_admin` / `openshift`)
| Argo CD (hub) | link:https://console-openshift-console.%HUB_DOMAIN%/k8s/ns/openshift-gitops/applications.argoproj.io~v1alpha1~Application[Argo CD (hub)] | Usuario taller
| ACM GitOps cluster | ACM → Infrastructure → Clusters → GitOps | Usuario taller
|===

== Demostración

. UI Argo CD: Application hub vs fuentes Application spoke usuario.
. Destacar generador ApplicationSet matrix east/west.
. Mostrar sync wave o estado health para app IE.
== Tu TODO (usuario %USER_NAME%)

* [ ] Encontrar tu Application IE en Argo CD y anotar estado sync
* [ ] Identificar fuente repo Git (Gitea usuario vs repo plataforma hub)
* [ ] Guardar progreso al final de este módulo

== Lab en vivo

* Terminal `oc` integrada en Showroom — usa contexto hub salvo que el módulo indique east/west.

== Dónde está definido este lab

NOTE: Las rutas referencian el repo GitOps `platform-hub-spoke-config` desplegado en **este** cluster. No copies fragmentos como manifiestos independientes — usa los enlaces de consola y verifica con `oc`.

[cols="2,3"]
|===
| UI / capability | Source in GitOps repo

| Hub Applications | `templates/component-applications.yaml` |
| ApplicationSet IE | `components/acm-hub-spoke/templates/applicationset.yaml` |
|===

Verificar en la terminal Showroom:

[source,bash]
----
oc get applications -n openshift-gitops 2>/dev/null | head -10
----

== Verificar

[cols="1,2,1"]
|===
| Check | Action | Expected

| Progress | Save checkboxes below | `POST /api/progress` returns OK
|===


++++
<div class="workshop-progress" data-module="16-openshift-gitops">
  <label><input type="checkbox" data-completed> Completé este módulo</label>
  <label><input type="checkbox" data-interest> Me interesa profundizar</label>
  <button type="button" onclick="saveWorkshopProgress('16-openshift-gitops')">Guardar progreso</button>
</div>
++++


TIP: Plan B — demo compartido en Developer Hub → System `hybrid-mesh-shared-demos` si falla el scaffolder.


++++
<div class="workshop-next-nav">
  <a href="17-service-mesh.html">Siguiente → 17 service mesh</a>
</div>
++++

