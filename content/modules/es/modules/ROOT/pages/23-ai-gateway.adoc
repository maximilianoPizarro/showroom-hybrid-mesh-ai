= AI Gateway — MaaS + Kuadrant

++++
<span class="workshop-time-badge">~25 min</span>
++++

image::23-ai-gateway.png[AI Gateway Kuadrant MaaS,960]

NOTE: Para snippets AWS/Azure ver xref:00-index.adoc#hybrid-integration[Módulo 00 — integración híbrida].

== Qué harás

En este módulo expones inferencia LLM **Model-as-a-Service (MaaS)** a través de un **AI Gateway** gobernado — no compartiendo rutas crudas de OpenShift AI. Harás:

. Abrir **Developer Hub → Catálogo → workshop-ai-gateway** y seguir enlaces a Topology, TechDocs y portal API Kuadrant.
. Rastrear GitOps en `components/workshop-kuadrant-apis/` — `HTTPRoute` Gateway API, `Gateway` Istio, y políticas Kuadrant.
. Crear API key para `%USER_NAME%` en UI Kuadrant y llamar `https://workshop-apis.%HUB_DOMAIN%/llm/v1/chat/completions` con Bearer token.
. Comparar latencia y errores contra MaaS directo desde notebook OpenShift AI (módulo 22).

*Prerrequisito:* módulo **22 OpenShift AI** — playground `maas-workshop` activo.

== Panorama


NOTE: **Ritmo del taller (~4 h hands-on)** — Cada módulo incluye pasos **Solo visualización** (~5–10 min) y **Hands-on** (lab completo). Omite Hands-on para un tour ejecutivo ~90 min.

El patrón **AI Gateway** centraliza cómo aplicaciones de fábrica, edge y partners consumen LLM en OpenShift. El tráfico entra por **`workshop-apis.%HUB_DOMAIN%`**, con **Gateway API** `HTTPRoute`, gateway Istio y políticas **Kuadrant** (`AuthPolicy`, `TokenRateLimitPolicy`).

**Por qué Kuadrant:** extiende Gateway API con gestión de APIs — keys, límites de uso y políticas en rutas que frontean inferencia. GitOps: `components/workshop-kuadrant-apis/`. Entidad catálogo **workshop-ai-gateway** documenta el flujo para `%USER_NAME%`.

**Solo visualización (~5 min):** Catálogo → **workshop-ai-gateway**; UI Kuadrant → forma de API key.

**Hands-on (~25 min):** Crear key, `curl` a `/llm/v1/chat/completions` con JSON chat-completions; comparar con MaaS directo desde notebook en `ai-%USER_NAME%`.

=== Profundizar

* link:https://www.kuadrant.io/docs/[Kuadrant — gestión de APIs en Kubernetes]
* link:https://docs.redhat.com/es/documentation/red_hat_openshift_ai_self-managed[Serving de modelos — OpenShift AI]
* link:https://docs.redhat.com/es/documentation/openshift_container_platform/4.16/html/service_mesh/gateway-api-for-service-mesh[Gateway API en OpenShift Service Mesh]
* link:https://developers.redhat.com/articles/2024/08/22/api-management-kubernetes-kuadrant[Gestión de APIs con Kuadrant]


== Acceso al producto — URLs y credenciales

NOTE: *Login del taller* — %USER_NAME% / `Welcome123!` (Keycloak en Developer Hub; htpasswd en consola OpenShift hub/east/west; DevSpaces en spoke east). Productos OAuth usan la misma identidad OpenShift.

[cols="2,3,2"]
|===
| Servicio | URL | Credenciales

| AI Gateway (MaaS LLM) | link:https://workshop-apis.%HUB_DOMAIN%/llm/v1/chat/completions[AI Gateway (MaaS LLM)] | API key Kuadrant
| UI Kuadrant | link:https://developer-hub.%HUB_DOMAIN%/kuadrant[UI Kuadrant] | Usuario taller
| Entidad catálogo | link:https://developer-hub.%HUB_DOMAIN%/catalog/default/component/workshop-ai-gateway[Entidad catálogo] | Usuario taller
|===

== Demostración

. Catálogo → **workshop-ai-gateway** → HTTPRoute en Topology.
. UI Kuadrant: crear API key; curl `/llm/v1/chat/completions`.
. Mostrar rutas GitOps AuthPolicy + TokenRateLimitPolicy.
== Tu TODO (usuario %USER_NAME%)

* [ ] Abrir catálogo **workshop-ai-gateway** y UI Kuadrant
* [ ] Crear API key y llamar `/llm/v1/chat/completions`
* [ ] Guardar progreso al final de este módulo

== Lab en vivo

* Terminal `oc` integrada en Showroom — usa contexto hub salvo que el módulo indique east/west.

== Dónde está definido este lab

NOTE: Las rutas referencian el repo GitOps `platform-hub-spoke-config` desplegado en **este** cluster. No copies fragmentos como manifiestos independientes — usa los enlaces de consola y verifica con `oc`.

[cols="2,3"]
|===
| UI / capability | Source in GitOps repo

| HTTPRoute + policies | `components/workshop-kuadrant-apis/` |
| Catalog | `catalog-ai-platform.yaml` → workshop-ai-gateway |
|===

Verificar en la terminal Showroom:

[source,bash]
----
oc get httproute -n hub-gateway-system 2>/dev/null | grep workshop
----

== Verificar

[cols="1,2,1"]
|===
| Check | Action | Expected

| Progress | Save checkboxes below | `POST /api/progress` returns OK
|===


++++
<div class="workshop-progress" data-module="23-ai-gateway">
  <label><input type="checkbox" data-completed> Completé este módulo</label>
  <label><input type="checkbox" data-interest> Me interesa profundizar</label>
  <button type="button" onclick="saveWorkshopProgress('23-ai-gateway')">Guardar progreso</button>
</div>
++++


TIP: Plan B — demo compartido en Developer Hub → System `hybrid-mesh-shared-demos` si falla el scaffolder.


++++
<div class="workshop-next-nav">
  <a href="24-mcp-gateway.html">Siguiente → 24 mcp gateway</a>
</div>
++++

