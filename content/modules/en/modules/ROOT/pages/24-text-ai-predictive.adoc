= Generative & Predictive Text

span.workshop-time-badge[~18 min]

NOTE: For the ROSA/AWS vs lab mapping see xref:00-index.adoc#hybrid-callout[Module 00 — hybrid callout table].

== Overview

Generative AI assists operators with natural-language summaries of alarms; predictive AI forecasts failures before downtime. On OpenShift, the ie-anomaly-alerter deployment watches Prometheus metrics from Industrial Edge sensors and emits alerts when statistical thresholds breach — a lightweight predictive pattern without mandatory KServe for this workshop track.

Optional KServe InferenceService resources on the hub demonstrate full model serving for custom scikit-learn or ONNX models trained in DS workspaces. MaaS playground endpoints let `pass:[%USER_NAME%]` test generative prompts for incident postmortems: "Summarize last hour Kafka lag and ACS violations for my namespace."

Connect predictive alerts to module 26 end-user apps: Camel routes can fan out anomaly events to line-dashboard overlays and NeuroFace notifications. This closes the loop from telemetry → ML/statistical detection → human + generative AI response on the same OpenShift footprint.

== Lab access — URLs & credentials

NOTE: *Workshop login* — pass:[%USER_NAME%] / `Welcome123!` (Keycloak for Developer Hub; htpasswd for OpenShift Console on hub, east, and west; DevSpaces on east spoke). OAuth products use the same OpenShift identity.

[cols="2,3,2"]
|===
| Service | URL | Credentials

| MaaS playground | `https://maas-rhdp.apps.maas.redhatworkshops.io/v1` | Facilitator token
| OpenShift AI dashboard | `https://rhods-dashboard-redhat-ods-applications.{hub_domain}` | OpenShift OAuth
| Anomaly alerter (east spoke) | ACM → `east` → `industrial-edge-tst-pass:[%USER_NAME%]` | Workshop user
|===

== Show and Tell

. Show ie-anomaly-alerter firing on threshold breach in metrics.
. MaaS prompt: generative summary of recent IE alerts.
. Mention optional KServe path for custom models.

== Your TODO (user pass:[%USER_NAME%])

* [ ] Confirm ie-anomaly-alerter deployment running on your spoke
* [ ] Generate one alarm summary prompt via MaaS playground
* [ ] Save progress at the end of this module

== Live lab

* Showroom integrated `oc` terminal — use hub context unless module says east/west.

== YAML behind the scenes

[cols="2,3,1"]
|===
| UI action | Git source | Kind

| Anomaly alerter | components/ie-anomaly-alerter/ | Deployment |
| Kafka metrics | industrial-edge-tst-all | Topic |
|===

[source,yaml]
----
# ie-anomaly-alerter correlates sensor anomalies
----

Verify:

[source,bash]
----
oc get deploy -n industrial-edge-tst-all | grep -i anomaly
----

== Verify

[cols="1,2,1"]
|===
| Check | Action | Expected

| Progress | Save checkboxes below | `POST /api/progress` returns OK
|===


++++
<div class="workshop-progress" data-module="24-text-ai-predictive">
  <label><input type="checkbox" data-completed> I completed this module</label>
  <label><input type="checkbox" data-interest> I want to learn more</label>
  <button type="button" onclick="saveWorkshopProgress('24-text-ai-predictive')">Save progress</button>
</div>
++++


TIP: Plan B — shared demo in Developer Hub → System `hybrid-mesh-shared-demos` if scaffolder fails.


++++
<div class="workshop-next-nav">
  <a href="25-neuroface.html">Next → 25 neuroface</a>
</div>
++++

