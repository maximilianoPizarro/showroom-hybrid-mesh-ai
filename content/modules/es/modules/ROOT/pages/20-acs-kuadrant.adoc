= ACS y Kuadrant

++++
<span class="workshop-time-badge">~18 min</span>
++++

image::images/20-acs-kuadrant.png[ACS and Kuadrant API security,600]

NOTE: Para snippets AWS/Azure ver xref:00-index.adoc#hybrid-integration[Módulo 00 — integración híbrida].

== Qué harás

Usarás **ACS Central** en este cluster (ruta `central-stackrox`) y **Developer Hub → Kuadrant** para solicitar API key de `workshop-apis`. Prueba con `curl` y tu key — no URLs genéricas de marketing.

== Panorama


NOTE: **Ritmo del taller (~4 h hands-on)** — Cada módulo incluye pasos **Solo visualización** (~5–10 min) y **Hands-on** (lab completo). Omite Hands-on para un tour ejecutivo ~90 min.

Red Hat Advanced Cluster Security ofrece escaneo de vulnerabilidades, benchmarks de cumplimiento y detección de amenazas runtime en clusters gestionados por ACM. Agentes SecuredCluster en spokes reportan a ACS Central en el hub; init bundles sincronizan vía jobs GitOps en `components/acs-init-bundle-sync/`.

Connectivity Link y Kuadrant extienden gestión de APIs al gateway del hub: AuthPolicy valida tokens, RateLimitPolicy protege backends y APIProduct publica APIs Industrial Edge para consumidores externos. El demo `demo-ie-api-product` en catálogo Plan B expone los mismos recursos Kuadrant sin scaffolding.

Como `%USER_NAME%`, verifica que ACS ve tus cargas spoke y prueba rutas APIProduct vía gateway del hub. Recuerda que ACS corre fuera del mesh ambient — este patrón de coexistencia es deliberado y coincide con despliegues ROSA + ACS en producción.


== Acceso al producto — URLs y credenciales

NOTE: *Login del taller* — %USER_NAME% / `Welcome123!` (Keycloak en Developer Hub; htpasswd en consola OpenShift hub/east/west; DevSpaces en spoke east). Productos OAuth usan la misma identidad OpenShift.

[cols="2,3,2"]
|===
| Servicio | URL | Credenciales

| ACS Central | link:https://central-stackrox.%HUB_DOMAIN%[ACS Central] | OAuth OpenShift
| Kuadrant — API keys | link:https://developer-hub.%HUB_DOMAIN%/kuadrant[Kuadrant — API keys] | Usuario taller → key para %USER_NAME%
| Workshop APIs (httpbin, REST, LLM) | link:https://workshop-apis.%HUB_DOMAIN%[Workshop APIs (httpbin, REST, LLM)] | `Authorization: Bearer <API_KEY>`
| Developer Hub — catálogo API | link:https://developer-hub.%HUB_DOMAIN%/catalog[Developer Hub — catálogo API] | Usuario taller
|===

== Demostración

. Overview ACS Central — violaciones y despliegues en spokes.
. Demo ruta APIProduct vía gateway hub con AuthPolicy.
. Recordar namespace ACS fuera del mesh ambient.
== Tu TODO (usuario %USER_NAME%)

* [ ] Verificar que ACS muestra tus cargas spoke en UI Central
* [ ] Probar ruta APIProduct vía gateway hub (o demo Plan B)
* [ ] Guardar progreso al final de este módulo

== Lab en vivo

* Terminal `oc` integrada en Showroom — usa contexto hub salvo que el módulo indique east/west.

== Dónde está definido este lab

NOTE: Las rutas referencian el repo GitOps `platform-hub-spoke-config` desplegado en **este** cluster. No copies fragmentos como manifiestos independientes — usa los enlaces de consola y verifica con `oc`.

[cols="2,3"]
|===
| UI / capability | Source in GitOps repo

| Workshop APIs | `components/workshop-kuadrant-apis/` |
| Hub gateway | `components/hub-gateway/` |
|===

Verificar en la terminal Showroom:

[source,bash]
----
oc get httproute,serviceentry -n hub-gateway-system 2>/dev/null | grep -i workshop | head -5
----

== Verificar

[cols="1,2,1"]
|===
| Check | Action | Expected

| Progress | Save checkboxes below | `POST /api/progress` returns OK
|===


++++
<div class="workshop-progress" data-module="20-acs-kuadrant">
  <label><input type="checkbox" data-completed> Completé este módulo</label>
  <label><input type="checkbox" data-interest> Me interesa profundizar</label>
  <button type="button" onclick="saveWorkshopProgress('20-acs-kuadrant')">Guardar progreso</button>
</div>
++++


TIP: Plan B — demo compartido en Developer Hub → System `hybrid-mesh-shared-demos` si falla el scaffolder.


++++
<div class="workshop-next-nav">
  <a href="21-finops-kubecost.html">Siguiente → 21 finops kubecost</a>
</div>
++++

